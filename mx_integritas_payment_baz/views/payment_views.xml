<odoo>
    <data>
        <record id="acquirer_form_paypal" model="ir.ui.view">
            <field name="name">acquirer.form.baz</field>
            <field name="model">payment.acquirer</field>
            <field name="inherit_id" ref="payment.payment_acquirer_form"/>
            <field name="arch" type="xml">
                <xpath expr='//group[@name="acquirer"]' position='inside'>
                    <group attrs="{'invisible': [('provider', '!=', 'baz')]}">
                        <field name="baz_afiliacion" attrs="{'required':[ ('provider', '=', 'baz'), ('state', '!=', 'disabled')]}"/>
                        <field name="baz_id_terminal" attrs="{'required':[ ('provider', '=', 'baz'), ('state', '!=', 'disabled')]}"/>
                    </group>
                </xpath>
                <xpath expr='//field[@name="journal_id"]' position='replace'>
                    <field name="journal_id" context="{'default_type': 'bank'}" attrs="{'required': [('state', '!=', 'disabled'), ('provider', '=', 'baz')]}"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
    
